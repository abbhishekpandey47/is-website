export const metadata = {
  title: "Context Engineering & Determinism in LLMs | White Paper",
  description: "Discover how context engineering enhances determinism in LLMs. Read Kubiya’s whitepaper for insights into building more predictable language models.",
};

export default function PlatformLayout({ children }) {
  return <>{children}</>;
}
